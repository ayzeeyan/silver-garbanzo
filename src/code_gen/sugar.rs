pub fn apply_all(_source: &mut String) {
    // 1. Dot Access Pass (t["key"] -> t.key)
    // 2. Local Function Pass
    // 3. Concat Simplify Pass
    // 4. Bool Return Simplify Pass

    // We modify `source` in-place
}
